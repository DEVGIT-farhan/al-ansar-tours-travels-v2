export interface Package {
  id: number;
  title: string;
  slug: string;
  badge: string;
  image: string;
  duration: string;
  price: string;
  rating: number;
  includes: string[];
  whatsappMessage: string;
}
